name: Rebuild property dataset

on:
  workflow_dispatch:
    inputs:
      counties:
        description: Comma-separated county slugs
        required: true
        default: dublin,carlow,kildare,wicklow
        type: string
      max_pages:
        description: Page limit per county
        required: true
        default: "10"
        type: string
      remove_legacy:
        description: After a successful scrape, remove review rows and old Sheet imports
        required: true
        default: false
        type: boolean
      min_valid_per_county:
        description: Minimum fresh valid rows required per county before cleanup
        required: true
        default: "5"
        type: string

permissions:
  contents: read

concurrency:
  group: property-database-write
  cancel-in-progress: false

jobs:
  rebuild:
    runs-on: [self-hosted, windows, x64, property-scraper]
    timeout-minutes: 330
    env:
      PYTHONPATH: backend
      DATABASE_URL: ${{ secrets.PROPERTY_DATABASE_URL }}
      SCRAPE_START_YEAR: "2025"
      SCRAPE_DELAY_SECONDS: "0.8"
      DETAIL_DELAY_SECONDS: "0.45"
      LOG_LEVEL: INFO
    steps:
      - uses: actions/checkout@v4

      - name: Locate system Python 3.13 or 3.12
        shell: powershell
        run: |
          $candidates = @(
            "C:\Program Files\Python313\python.exe",
            "C:\Program Files\Python312\python.exe",
            "C:\Python313\python.exe",
            "C:\Python312\python.exe",
            "$env:LOCALAPPDATA\Programs\Python\Python313\python.exe",
            "$env:LOCALAPPDATA\Programs\Python\Python312\python.exe"
          )
          $python = $candidates | Where-Object { Test-Path $_ } | Select-Object -First 1
          if (-not $python) {
            $launcher = Get-Command py.exe -ErrorAction SilentlyContinue
            if ($launcher) {
              foreach ($selector in @("-3.13", "-3.12")) {
                $resolved = & $launcher.Source $selector -c "import sys; print(sys.executable)" 2>$null
                if ($LASTEXITCODE -eq 0 -and $resolved -and (Test-Path $resolved)) {
                  $python = $resolved
                  break
                }
              }
            }
          }
          if (-not $python) {
            $command = Get-Command python.exe -ErrorAction SilentlyContinue
            if ($command) { $python = $command.Source }
          }
          if (-not $python) {
            throw "Python 3.13 or 3.12 x64 was not found. Install it for all users, then restart the GitHub runner service."
          }
          $version = & $python -c "import sys; print(f'{sys.version_info.major}.{sys.version_info.minor}')"
          if ($version -notin @("3.13", "3.12")) {
            throw "Python 3.13 or 3.12 is required; found $version at $python"
          }
          "PYTHON_EXE=$python" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
          $pythonDir = Split-Path $python
          $pythonDir | Out-File -FilePath $env:GITHUB_PATH -Append -Encoding utf8
          (Join-Path $pythonDir "Scripts") | Out-File -FilePath $env:GITHUB_PATH -Append -Encoding utf8
          & $python --version

      - name: Install Python dependencies
        shell: powershell
        run: |
          & $env:PYTHON_EXE -m pip install --upgrade pip
          & $env:PYTHON_EXE -m pip install -r backend/requirements.txt
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Install Playwright Chromium
        shell: powershell
        run: |
          & $env:PYTHON_EXE -m playwright install chromium
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Scrape fresh Daft sold listings
        shell: powershell
        run: |
          & $env:PYTHON_EXE -m property_tracker.scraper --mode full --counties "${{ inputs.counties }}" --max-pages "${{ inputs.max_pages }}"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Audit all stored records
        shell: powershell
        run: |
          & $env:PYTHON_EXE -m property_tracker.data_maintenance audit
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Remove invalid and legacy rows after successful scrape
        if: ${{ inputs.remove_legacy }}
        shell: powershell
        run: |
          & $env:PYTHON_EXE -m property_tracker.data_maintenance cleanup --counties "${{ inputs.counties }}" --remove-legacy --min-valid-per-county "${{ inputs.min_valid_per_county }}"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
